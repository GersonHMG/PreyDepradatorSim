#pragma once


namespace Config{
    // World config
    const int WORLD_DIMENSION = 100;

    // Screen config
    const int SCREEN_WIDTH = 800;
    const int SCREEN_HEIGHT = 800;
    const int FPS = 60;
}